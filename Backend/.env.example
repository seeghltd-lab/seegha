# Machine-to-machine auth for the external Backup-auto service.
# Long random secret sent as the `x-backup-api-key` header when the
# unattended backup cron calls POST /data-export/export.
BACKUP_SERVICE_API_KEY=
